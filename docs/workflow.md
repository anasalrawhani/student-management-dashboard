## `docs/workflow.md`

```markdown
# Git Workflow

This document explains the professional Git workflow used in this project.

## Branch Strategy

```text
main = stable production code
develop = integration branch
feature/* = new features
bugfix/* = bug fixes
release/* = release preparation